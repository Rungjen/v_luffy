import Vue from "vue";

import {
	Menu,
	Modal,
	FormModel,
	Input,
	Icon,
	Button,
	Table,
	Divider,
	Select,
	DatePicker,
	Carousel,
} from 'ant-design-vue';


Vue.use(FormModel)
		.use(Input)
		.use(Icon)
		.use(Button)
		.use(Table)
		.use(Divider)
		.use(Select)
		.use(DatePicker)
		.use(Modal)
		.use(Menu)
		.use(Carousel)
